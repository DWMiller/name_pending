let config = {
    h: 0,
    w: 0,
    scale: 1,
    obstacleCount: 50,
    canvasBlurRate: 0.2, //between 0-1, 1 being instant clear
    objects: {
        particle: {
            scale: 1
        },
        mirror: {
            scale: 10
        },
        projector: {
            scale: 5
        }
    }
};

